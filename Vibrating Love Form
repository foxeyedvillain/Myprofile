<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vibrating Love</title>
  <style>
    /* General Page Styling */
    body {
      font-family: Arial, Helvetica, sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Form Container */
    form {
      background-color: white;
      padding: 30px 40px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      width: 380px;
    }

    /* Labels and Inputs */
    .container {
      margin-bottom: 15px;
    }

    label {
      display: inline-block;
      width: 130px;
      font-weight: bold;
      color: #333;
    }

    input[type="text"],
    input[type="email"],
    input[type="date"],
    input[type="number"],
    select {
      width: 200px;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 14px;
      transition: border-color 0.3s ease;
    }

    input:focus,
    select:focus {
      border-color: #ff4b5c;
      outline: none;
    }

    /* Gender Section */
    .gender-group {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .gender-option {
      display: flex;
      align-items: center;
      gap: 5px;
    }

    /* Buttons Section */
    #submit {
      text-align: center;
    }

    button {
      background-color: #ff4b5c;
      color: white;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      font-size: 15px;
      cursor: pointer;
      margin-right: 10px;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    button:hover {
      background-color: #e13b4a;
      transform: scale(1.05);
    }

    button[type="reset"] {
      background-color: #888;
    }

    button[type="reset"]:hover {
      background-color: #666;
    }

    /* Responsive Design */
    @media (max-width: 480px) {
      form {
        width: 90%;
        padding: 20px;
      }

      label {
        display: block;
        margin-bottom: 5px;
      }

      input,
      select {
        width: 100%;
      }

      .gender-group {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>
</head>
<body>
  <form method="post" id="time_table">
    <h2 style="text-align:center; color:#ff4b5c;">Vibrating Love Registration</h2>

    <div class="container">
      <label for="userName">User Name:</label>
      <input type="text" id="userName" name="userName" placeholder="Enter your Name" required>
    </div>

    <div class="container">
      <label>Gender:</label>
      <div class="gender-group">
        <div class="gender-option">
          <input type="radio" id="male" name="gender" value="male" required>
          <label for="male">Male</label>
        </div>
        <div class="gender-option">
          <input type="radio" id="female" name="gender" value="female">
          <label for="female">Female</label>
        </div>
        <div class="gender-option">
          <input type="radio" id="other" name="gender" value="other">
          <label for="other">Other</label>
        </div>
      </div>
    </div>

    <div class="container">
      <label for="email">Email:</label>
      <input type="email" name="email" id="email" placeholder="Enter your Email" required>
    </div>

    <div class="container">
      <label for="dob">Date of Birth:</label>
      <input type="date" id="dob" name="dob" required>
    </div>

    <div class="container">
      <label for="qualification">Qualification:</label>
      <select name="qualification" id="qualification">
        <option value="Post Graduate">Post Graduate</option>
        <option value="Under Graduate" selected>Under Graduate</option>
      </select>
    </div>

    <div class="container">
      <label for="phoneNumber">Phone Number:</label>
      <input type="number" maxlength="10" name="phoneNumber" id="phoneNumber" placeholder="" required>
    </div>

    <div id="submit">
      <button type="submit">Submit</button>
      <button type="reset">Reset</button>
    </div>
  </form>
</body>
</html>
